Future<void> adapterConfiguration() async {
  // if (!Hive.isAdapterRegistered(UserRegisterDetailsAdapter().typeId)) {
  //   Hive.registerAdapter(UserRegisterDetailsAdapter());
  // }

  // await Hive.openBox<UserRegisterDetails>('customerData');
}
